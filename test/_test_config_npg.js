var config = {
  useWebFront: false, // Put it to false if you don't want a web interface.

  useFixedKey: true, // When put this to true, >
  key1: 'aaa', // > you can set two fixed key here.
  key2: 'bbb'
};

module.exports = config;
